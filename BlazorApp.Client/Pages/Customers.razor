@page "/customers"
@inject IJSRuntime JS

@rendermode @(new InteractiveWebAssemblyRenderMode(prerender: false))

@inject IRepository CustomersService;

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <div class="row">
                        <div class="col-10">
                            <h3>Customers List</h3>
                        </div>
                        <div class="col-2">
                            <button type="button" class="btn btn-info float-end" @onclick="() => AddNewCustomer()">
                                <i class="fas fa-plus"></i> Add New Entry
                            </button>
                        </div>
                    </div>
                </div>
                
                <div class="card-body">
                    @if (CustomersList?.Count == 0)
                    {
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>Company Name</th>
                                        <th>Contact Name</th>
                                        <th>Address</th>
                                        <th>City</th>
                                        <th>Region</th>
                                        <th>Postal Code</th>
                                        <th>Country</th>
                                        <th>Phone No.</th>
                                        <th>#Ops</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td colspan="9">
                                            <p><em>Loading, please wait ... </em></p>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>Company Name</th>
                                        <th>Contact Name</th>
                                        <th>Address</th>
                                        <th>City</th>
                                        <th>Region</th>
                                        <th>Postal Code</th>
                                        <th>Country</th>
                                        <th>Phone No.</th>
                                        <th>#Ops</th>
                                    </tr>
                                </thead>
                                <tbody style="font-size: 10px">
                                    
                                    @foreach(var customer in CustomersList)
                                    {
                                        <tr>
                                            <td>@customer.CompanyName</td>
                                            <td>@customer.ContactName</td>
                                            <td>@customer.Address</td>
                                            <td>@customer.City</td>
                                            <td>@customer.Region</td>
                                            <td>@customer.PostalCode</td>
                                            <td>@customer.Country</td>
                                            <td>@customer.Phone</td>
                                            <td>
                                                <div class="hstack gap-1">
                                                    <button class="btn btn-warning" @onclick="() => HandleEdit(customer.Id)"><i class="fas fa-edit"></i></button>
                                                    <button class="btn btn-danger" @onclick="() => PromptDelete(customer.Id)"><i class="fas fa-trash"></i></button>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot style="font-size:10px">
                                    <p><strong>Total Entries:</strong> @CustomersList.Count.ToString("N0") </p>
                                </tfoot>
                            </table>
                            <div class="col-12 mt-4 text-center">
                                <button class="btn btn-primary" @onclick="PreviousPage" disabled="@(_page == 1)"><i class="fas fa-arrow-left"></i></button>
                                <span>&nbsp; Page @_page &nbsp;</span>
                                <button class="btn btn-primary" @onclick="NextPage" disabled="@(_page * _pageSize >= _totalCount)"><i class="fas fa-arrow-right"></i></button>
                            </div>

                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>


